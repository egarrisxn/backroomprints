export type OneNavLink = {
  href: string;
  label: string;
};

export type OneService = {
  title: string;
  description: string;
};

export type OneFeaturedProject = {
  title: string;
  category: string;
  description: string;
};

export type OneProcessStep = {
  number: string;
  title: string;
  description: string;
};
